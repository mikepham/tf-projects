[
  {
    "cpu": 256,
    "essential": true,
    "image": "nginx:alpine",
    "memory": 512,
    "name": "default",
    "portMappings": [
      {
        "containerPort": 80,
        "hostPort": 80
      }
    ],
    "secrets": [
      {
        "name": "${secret_name}",
        "valueFrom": "${secret_arn}"
      }
    ]
  }
]
